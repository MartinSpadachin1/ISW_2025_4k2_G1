[tool.pytest.ini_options]
# This tells pytest where to look for source code.
pythonpath = ["."]
# The default location for tests
testpaths = ["test"]
filterwarnings = [
    "ignore:The default date adapter is deprecated:DeprecationWarning"
]